The language used to move the Robotic Arm consist on the following syntax:

pick a b
move a b
drop a b
move pick a b c d
move drop a b c d

Where "a", "b", "c" & "d" are all integers between 0 and 10, excluding the latter.
